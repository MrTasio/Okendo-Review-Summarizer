{
  "name": "review-summary-api",
  "version": "1.0.0",
  "description": "Vercel API for generating review summaries using Google Gemini or Hugging Face",
  "main": "api/generate-summary.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}
